<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Posts</title>
</head>
<body>
    <h1>Blog Posts</h1>
    <div id="blog-posts"></div>
    <div class="blog-post">
        <h2>{{ blogpost.title }}</h2>
        <p>Author: {{ blogpost.author }}</p>
        <p>Date Published: {{ blogpost.date_published }}</p>
        <p>{{ blogpost.article }}</p>

        <h2>Comments:</h2>
        {{#each blogpost.Comments}}
        <div class="comment">
          <p>{{this.text}}</p>
          <p>Posted By: {{this.User.name}}</p>
          <p>Posted at: {{this.createdAt}}</p>
        </div>
        {{/each}}
    </div>
    <div class="card">
  <h2>{{ title }}</h2>
  <p>{{ content }}</p>
  <!-- Add Comments button -->
  <button class="comment-button" data-post-id="{{ id }}">Comments</button>
  <!-- Comment form -->
  <form id="comment-form-{{ id }}" class="hidden comment-form" data-post-id="{{ id }}">
    <textarea class="comment-textarea" placeholder="Write your comment..."></textarea>
    <button type="submit" class="comment-submit">Submit Comment</button>
  </form>
</div>
        <script src="/js/Blogpost.js"></script>
          <script src="/js/comment.js"></script>
</body>
</html>