<content>
  <div id="post1" class="card">
    <div id="blog-posts-container">
      {{#if userBlogData}}
      <div class="card">
        <h2>{{ userBlogData.title }}</h2>
        <p>
          "{{ userBlogData.article }}"<br>
          by: You<br>
          {{ userBlogData.date }}
        </p>
      </div>
      {{/if}}
    </div>
    <div class="card">
  <h2>{{ title }}</h2>
  <p>{{ content }}</p>

  <button class="comment-button" data-post-id="{{ blogpost.id }}">Comments</button>

  <form id="comment-form-{{ blogpost.id }}" class="hidden comment-form" data-post-id="{{ blogpost.id }}">
    <textarea class="comment-textarea" placeholder="Write your comment..."></textarea>
    <button type="submit" class="comment-submit">Submit Comment</button>
  </form>
</div>
    <h2>Blog 1</h2>

    <p>
      "Why MVC Is So Important",<br>
      by: Bob Green<br>
      June 6,2021<br>
      MVC allows developers to maintain a true seperation of concerns, devising their code between the Model layer for
      data,<br>
      the View layer for design and the Controller layer for application logic.
    </p>
  </div>
<div class="card">
  <h2>{{ title }}</h2>
  <p>{{ content }}</p>

  <button class="comment-button" data-post-id="{{ blogpost.id }}">Comments</button>

  <form id="comment-form-{{ blogpost.id }}" class="hidden comment-form" data-post-id="{{ blogpost.id }}">
    <textarea class="comment-textarea" placeholder="Write your comment..."></textarea>
    <button type="submit" class="comment-submit">Submit Comment</button>
  </form>
</div>
  <div id="post2" class="card">
    <h2>Blog 2</h2>
    <p>
      "What is MVC?"<br>
      by: Trygve Reenskaug<br>
      July 9th, 1970<br>
      MVC stands for Model-View-Controller, and it's a software architectural pattern commonly used in designing<br>
      and organizing code for applications, especially those with graphical user interfaces.
      whereas authorization means being allowed access to the system.</p>
  </div>
<div class="card">
  <h2>{{ title }}</h2>
  <p>{{ content }}</p>
  <!-- Add Comments button -->
  <button class="comment-button" data-post-id="{{ blogpost.id }}">Comments</button>
  <!-- Comment form -->
  <form id="comment-form-{{ blogpost.id }}" class="hidden comment-form" data-post-id="{{ blogpost.id }}">
    <textarea class="comment-textarea" placeholder="Write your comment..."></textarea>
    <button type="submit" class="comment-submit">Submit Comment</button>
  </form>
</div>
  <div id="post3" class="card">
    <h2>Blog 3</h2>
    <p>
      "What is Sequelize?"<br>
      by: Sascha Depold<br>
      January 10, 2012<br>
      Sequelize is a popular Object-Relational Mapping (ORM) library for Node.js. It provides a way to<br>
      interact with relational databases, such as MySQL, PostgreSQL, SQLite, and Microsoft SQL Server, using<br>
      JavaScript code and object-oriented concepts.</p>
  </div>
<div class="card">
  <h2>{{ title }}</h2>
  <p>{{ content }}</p>
  <!-- Add Comments button -->
  <button class="comment-button" data-post-id="{{ blogpost.id }}">Comments</button>
  <!-- Comment form -->
  <form id="comment-form-{{ blogpost.id }}" class="hidden comment-form" data-post-id="{{ blogpost.id }}">
    <textarea class="comment-textarea" placeholder="Write your comment..."></textarea>
    <button type="submit" class="comment-submit">Submit Comment</button>
  </form>
</div>
<script src="/js/comment.js"></script>
</content>